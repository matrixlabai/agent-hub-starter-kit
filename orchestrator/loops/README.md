# Loops

Loops define when verified workflows run, their concurrency, stop conditions, and durable state.
They never duplicate a skill's execution method and never become active merely because a loop
definition exists. Enabling a recurring job is a separate, explicit action.
