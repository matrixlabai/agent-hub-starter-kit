# Workflows

Workflows coordinate multiple bounded tasks with explicit dependencies. Inputs, prompts, outputs,
and verification receipts should live in files so interrupted runs can resume without replaying
completed work.
