# Delegation policies

Local policies decide which main-agent identities may use which routes, maximum concurrency,
timeouts, reasoning levels, and write boundaries. Keep credential values and private account names
out of policy files. Default to deny when caller identity or route capability is unknown.
