# Worker brief contract

Every delegated task contains only:

1. exact objective
2. exact working directory
3. required input and target paths
4. allowed writes
5. explicit exclusions
6. required artifact or compact response
7. verification and stopping condition

Workers return conclusions, evidence, changed paths, verification results, and blockers. They do
not own canonical decisions or the final user-facing claim.
