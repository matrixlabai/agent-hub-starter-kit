# Platform instructions

Platform files are machine-specific glue. Parameterize installation roots, work storage, hostnames,
ports, and optional tools through ignored local configuration. Never copy another machine's paths,
account state, or secret loaders into this directory.

PowerShell scripts must parse on Windows PowerShell 5.1 and PowerShell 7 when both are supported.
