name: safety

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  public-safety:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Agent Hub doctor
        shell: pwsh
        run: ./doctor.ps1
