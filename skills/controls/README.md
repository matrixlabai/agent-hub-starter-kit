# Control skills

A control skill owns one external platform or execution boundary. It exposes atomic operations,
validation, dry-run behavior, safety tiers, error handling, and read-back verification.

Controls are domain-blind. They contain no tenant identity, account ID, schedule, brand voice,
business rule, local session state, or credential value.
