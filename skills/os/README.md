# OS skills

An OS skill owns an end-to-end lifecycle from validated input to a verified deliverable. It may
coordinate control and general skills but never duplicates their platform logic.

Every OS skill defines a finish line, required inputs, canonical outputs, ordered stages,
boundaries and approval gates, resumable state, and verification evidence.
